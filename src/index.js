import React from 'react';
import ReactDOM from 'react-dom/client';
import './HomePage.css';
import App from './main/java/com/example/animationcreator/components/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
